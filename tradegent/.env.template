# ═══════════════════════════════════════════════
# Nexus Light Trading Platform v2.1 - Environment
# Copy to .env and fill in values
# ═══════════════════════════════════════════════

# Interactive Brokers
IB_USER=your_ib_username
IB_PASS=your_ib_password
IB_ACCOUNT=DU_PAPER
VNC_PASS=nexus123

# Anthropic (only needed if Claude Code uses API key auth instead of interactive session)
# ANTHROPIC_API_KEY=sk-ant-...

# OpenAI (only needed if using OpenAI embeddings — not needed with Ollama)
# OPENAI_API_KEY=sk-...

# LightRAG LLM/Embedding (uses local Ollama by default)
# Override models if needed:
# LIGHTRAG_LLM_MODEL=qwen3:14b
# LIGHTRAG_EMBED_MODEL=nomic-embed-text

# PostgreSQL (shared: LightRAG public schema + nexus schema)
PG_USER=lightrag
PG_PASS=changeme_pg_password
PG_DB=lightrag
PG_HOST=localhost
PG_PORT=5433

# Neo4j
NEO4J_PASS=changeme_neo4j_password

# Orchestrator limits
MAX_DAILY_ANALYSES=15
MAX_DAILY_EXECUTIONS=5
CLAUDE_TIMEOUT=600
LIGHTRAG_URL=http://localhost:9621
